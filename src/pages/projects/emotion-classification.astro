---
import BaseLayout from "../../layouts/BaseLayout.astro";

const base = import.meta.env.BASE_URL || '/';
const withBase = (path: string) =>
  base === '/' ? path : base.replace(/\/$/, '') + path;
---

<BaseLayout title="Emotion Classification Model" sideBarActiveItemID="projects">
  <article class="prose max-w-none">
    <div class="flex flex-col md:flex-row md:items-center gap-4 mb-6">
      <img
        src={withBase('/logos/cornell-logo.png')}
        alt="Cornell University logo"
        class="max-h-20 md:max-h-24"
        loading="lazy"
      />
      <h1 class="text-3xl font-bold leading-tight mt-4">
        Emotion Classification Model (NLP)
      </h1>
    </div>

    <p class="text-base leading-relaxed">
      Engineered a full NLP pipeline trained on a 50K-sample dataset, integrating
      GloVe embeddings, BiLSTM with Attention, CNN layers, and XGBoost to achieve
      <strong>95% accuracy</strong> across 20+ emotion classes.
    </p>

    <section class="mt-8">
      <h2 class="text-2xl font-semibold">What I built</h2>
      <ul class="list-disc ml-6 space-y-2">
        <li>
          Experimented with feature engineering, embedding strategies, sequence
          architectures, and hybrid deep-learning + classical ML approaches to find
          the best accuracy–latency tradeoff.
        </li>
        <li>
          Conducted robustness and bias analyses to ensure consistent performance
          across demographic groups and reduce distributional drift.
        </li>
        <li>
          Built model interpretability visualizations (saliency, attention maps)
          and shared insights with cross-functional partners.
        </li>
      </ul>
    </section>

    <section class="mt-8">
      <h2 class="text-2xl font-semibold">Skills I strengthened</h2>
      <div class="flex flex-wrap gap-2">
        <span class="badge badge-outline">Deep Learning</span>
        <span class="badge badge-outline">NLP Pipelines</span>
        <span class="badge badge-outline">Sequence Modeling</span>
        <span class="badge badge-outline">Model Evaluation & Bias Analysis</span>
        <span class="badge badge-outline">Python & ML Engineering</span>
      </div>
      <p class="mt-4">
        This project strengthened my understanding of how embedding representations,
        sequence encoders, attention mechanisms, and classical ML models interact.
        It also taught me how to evaluate robustness, visualize model behavior, and
        communicate insights to non-technical collaborators.
      </p>
    </section>
  </article>
</BaseLayout>
