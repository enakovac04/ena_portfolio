---
import BaseLayout from "../../layouts/BaseLayout.astro";
import HorizontalCard from "../../components/HorizontalCard.astro";

const base = import.meta.env.BASE_URL || '/';
const withBase = (path: string) => base === '/' ? path : base.replace(/\/$/, '') + path;

---

<BaseLayout title = "Projects" sideBarActiveItemID="projects">
  <div>
    <div class="text-3xl w-full font-bold mb-5">Projects</div>
  </div>

  <HorizontalCard
    title="WebSocket Management Service (AWS)"
    img={withBase('/logos/aws.svg')}
    desc="Designed and validated a WebSocket management service on AWS ECS handling 6K+ concurrent connections and 8MB payloads; implemented scalable throttling and persistence mechanisms and conducted large-scale load testing."
    url={withBase('/projects/websocket-management')}
    target="_self"
    tags={['AWS','WebSockets','ECS']}
    badge="INFRA"
  />
  <div class="divider my-0"></div>

  <HorizontalCard
    title="MCP Server for Automation (AWS)"
    img={withBase('/logos/aws.svg')}
    desc="Built an MCP server integrating 38 APIs for natural-language automation, reducing manual UI interaction time by ~30% and enabling reproducible automated workflows for product teams."
    url={withBase('/projects/mcp-server')}
    target="_self"
    tags={['AWS','Automation','NLP']}
    badge="AUTOMATION"
  />
  <div class="divider my-0"></div>

  <HorizontalCard
    title="Database Development (Java)"
    img={withBase('/logos/cornell-logo.png')}
    desc="Architected and deployed a SQL database managing 100K+ records, optimized large-scale queries for three core data models, and built a dynamic query planner to choose efficient algorithms across dataset sizes."
    url={withBase('/projects/database-development')}
    target="_self"
    tags={['Java','SQL','PostgreSQL']}
  />
  <div class="divider my-0"></div>

  <HorizontalCard
    title="Emotion Classification Model (NLP)"
    img={withBase('/logos/cornell-logo.png')}
    desc="Engineered an end-to-end NLP pipeline and deep-learning model on a 50K-sample dataset combining GloVe, BiLSTM+Attention, CNN layers, and XGBoost; reached 95% accuracy over 20+ emotion classes."
    url={withBase('/projects/emotion-classification')}
    target="_self"
    tags={['Python','Keras','NLP']}
    badge="ML"
  />
  <div class="divider my-0"></div>

  <HorizontalCard
    title="Distributed Messaging System (OCaml)"
    img={withBase('/logos/cornell-logo.png')}
    desc="Built a distributed chat system supporting 1,000+ concurrent clients with asynchronous message routing and socket-level concurrency; achieved <500ms latency and 99% delivery reliability."
    url={withBase('/projects/distributed-messaging')}
    target="_self"
    tags={['OCaml','Distributed','Networking']}
  />
</BaseLayout>
