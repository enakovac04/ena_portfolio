---
const { sideBarActiveItemID } = Astro.props;
// Highlight active sidebar item with a light background and normal text color
const activeClass = "bg-base-100 text-base-content";

// Helper to prepend base path to routes
const base = import.meta.env.BASE_URL || '/';
const withBase = (path: string) => base === '/' ? path : base.replace(/\/$/, '') + path;
---

<ul class="menu grow shrink menu-md overflow-y-auto">
    <li><a class="py-3 text-base" id="home" href={withBase('/')}>Home</a></li>
    <li><a class="py-3 text-base" id="projects" href={withBase('/projects')}>Projects</a></li>
    <li><a class="py-3 text-base" id="education" href={withBase('/education')}>Education</a></li>
    <li><a class="py-3 text-base" id="experience" href={withBase('/experience')}>Experience</a></li>
    <li><a class="py-3 text-base" id="cv" href={withBase('/cv')}>CV</a></li>
    <!-- Contact moved to footer; sidebar contact item removed -->
</ul>

<script define:vars={{ sideBarActiveItemID: sideBarActiveItemID, activeClass: activeClass }}>
const activeItemElem = document.getElementById(sideBarActiveItemID);
activeItemElem && activeItemElem.classList.add(activeClass);
</script>
